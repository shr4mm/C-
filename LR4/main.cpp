#include <iostream>
#include <iomanip>
#include <cmath>
using namespace std;
int main() {
    //#1
    /*Як працює програма:
Користувач вводить число
value
value.
Програма перев?ряє його на в?дпов?дн?сть одн?й ?з чотирьох умов, використовуючи лог?чн? оператори:
&& (?),
|| (або).
Якщо хоча б одна умова виконується,
зм?нна answer стає true, ? виводиться пов?домлення "ЦЕ ПРАВДА :)". ?накше виводиться "ЦЕ НЕ ПРАВДА :(".
*/
    bool answer; // Зм?нна для збереження результату
    int value;   // Введене число

    std::cout << "Введ?ть число: ";
    std::cin >> value;

    // Формула для перев?рки умов
    answer = (value >= 0 && value < 10) ||
             (value * 2 < 20 && value - 2 > -2) ||
             (value - 1 > 1 && value / 2 < 10) ||
             (value == 111);

    // Виведення результату
    std::cout << (answer ? "ЦЕ ПРАВДА :)" : "ЦЕ НЕ ПРАВДА :(") << std::endl;
    //№2
    // Оголошення п'яти зм?нних типу float
    float num1, num2, num3, num4, num5;

    // Введення значень
    cin >> num1 >> num2 >> num3 >> num4 >> num5;

    // Вив?д чисел у потр?бному формат?
    cout << num1 << endl;                            // Вивести як є
    cout << fixed << setprecision(2) << num2 << endl; // Вивести з 2 знаками п?сля коми
    cout << setprecision(6) << num3 << endl;         // Вивести з максимальною точн?стю
    cout << setprecision(2) << num4 << endl;         // Вивести округлене до 2 знак?в
    cout << setprecision(0) << num5 << endl;         // Вивести округлене до ц?лого числа
    //№3
    /*Завдання:
    Запитати у користувача два ц?л? числа.
    Розд?лити 1 на кожне з чисел (перетворивши їх у числа типу float перед д?ленням).
    Перев?рити, чи р?зниця результат?в не перевищує 0.000001 (epsilon).
    Вивести результат пор?вняння.*/
    const float epsilon = 0.000001; // Допустима похибка (epsilon)

    // Введення двох ц?лих чисел
    cout << "Введ?ть перше число: ";
    cin >> num1;
    cout << "Введ?ть друге число: ";
    cin >> num2;

    // Перетворення на float та обчислення
    float result1 = 1.0f / num1;
    float result2 = 1.0f / num2;

    // Пор?вняння з використанням epsilon
    if (fabs(result1 - result2) < epsilon) {
        cout << "Результати р?вн? (з точн?стю до 0.000001 epsilon)" << endl;
    } else {
        cout << "Результати не р?вн? (з точн?стю до 0.000001 epsilon)" << endl;
    }
    //№4
    /*код, який отримує чотири числа в?д користувача, перев?ряє,
     *чи знаходяться вони в межах в?д 1 до 255, ? об'єднує їх у вигляд? IP-адреси:*/
    cout << "Введ?ть перше число: ";
    cin >> num1;
    cout << "Введ?ть друге число: ";
    cin >> num2;
    cout << "Введ?ть третє число: ";
    cin >> num3;
    cout << "Введ?ть четверте число: ";
    cin >> num4;

    // Перев?рка, чи вс? числа знаходяться в д?апазон? в?д 1 до 255
    if ((num1 >= 1 && num1 <= 255) &&
        (num2 >= 1 && num2 <= 255) &&
        (num3 >= 1 && num3 <= 255) &&
        (num4 >= 1 && num4 <= 255)) {
        // Формування та вив?д IP-адреси
        cout << num1 << "." << num2 << "." << num3 << "." << num4 << endl;
        } else {
            // Помилка, якщо хоча б одне число не в д?апазон?
            cout << "Помилка: вс? числа повинн? бути в межах в?д 1 до 255!" << endl;
        }

    return 0;
}
